{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to application",
            "type": "lldb",
            "request": "attach",
            "pid": "${command:pickMyProcess}"
        }
    ]
}
